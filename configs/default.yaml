task:
  dataset: moon
  n: 5000
  m: 500
  train_ratio: 0.7

graph:
  knn_k_divisor: 10.0
  eball_percentile: 5.0
  eball_scaling_factor: 2.7
  K: 900

training:
  epochs: 100
  lr: 0.002

runtime:
  seed: 0

visualization:
  keep_percentile: 97.0
  fallback_keep_percentile: 95.0
  min_keep_ratio: 0.5